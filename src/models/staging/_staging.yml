version: 2

models:
  # NOTE - These are documented in sequential order
  - name: stg__setup
    description: ""
    columns:
      - name: dbt_id
        tests:
          - unique
          - not_null
      - name: profile_id
      - name: maker
      - name: name
      - name: style
      - name: standard_styles
      - name: country_of_origin
      - name: proof
      - name: rating
      - name: my_review
      - name: flavor_profile

  - name: stg__long
    description: ""
    tests:
      - unique:
          column_name: "( dbt_id || '-' || note )"
    columns:
      - name: dbt_id
        tests:
          - not_null
      - name: profile_id
      - name: standard_styles
      - name: country_of_origin
      - name: proof
      - name: rating
      - name: my_review
      - name: note