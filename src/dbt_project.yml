name: 'whiskey_ml'
version: '1.0.0'

profile: 'whiskey_ml'

model-paths: ["models"]
test-paths: ["tests"]
macro-paths: ["macros"]

clean-targets:       
  - "target"
  - "dbt_packages"

vars:
  records: complete

models:
  whiskey_ml:
    staging:
      +schema: staging
      +materialized: table
    production:
      +schema: analytics
      +materialized: table
